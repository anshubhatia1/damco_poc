[Music] so now that we have this context of the decoder only models we'll see the first uh decoder only model which is uh a generative pre-train Transformer is also popularly known as GPT and it has of course a family of models now the latest one being GPT 4 but we look at the first paper which came out in the series which is the original GPT model which would be much smaller than the current uh GPD models which are out so let's look at what it looks like so we saw what one block of the decoder only model looks like right we just saw one block there now I could just Stack Up N such blocks okay so I'm going to Stack Up N such blocks and we now know the game right so the way it works is that you give it an input it computes this representation U it computes a refined representation for each of the inputs right and it continues this till the end and at the end it will predict a probability distribution yes right so it's predicting the new representation at every layer and at the end it will use that to predict a probability distribution and that's exactly what we wanted does that make sense right and what we want at training time that's what we'll see now so let me just uh make sure that all the notation is understood it's actually very straightforward but the input layer which is the Ed zero layer okay it has a sequence of length T right and each word is represented by an embedding of size D model right so it could be 128 64 we'll see what it was for gp2 but it could be any 64 128 any of the numbers that you want to choose right and so that is the input and remember that the whole computation is happening in parallel while we have this triangular mask applied so that at every stage you only see the previous inputs and nothing which comes afterwards right so you are at one go passing the entire input sequence which is known to you during training to the model but you know that when you are Computing this guy it has not seen anything which comes after it is that clear right that the masking is taken care of so now at the end you simultaneously predicting all these probabilities and your job is to maximize each of these for the correct word right so probability of I should be maximum Here Am should be maximum here going should be maximum here home should be maximum here and so on right so all of these probabilities you need to maximize okay and that at every level you're Computing a new hidden representation which is shown by this so it's the representation coming out from the elith Transformer block which takes the input from from the L minus one Transformer block the first Transformer block always takes input from h0 which is just your raw input the word tics right and uh then at the final layer the nth layer you will apply the soft Max whatever the nth layer outputs you will pass pass it through a linear layer and then apply soft Max on top of that and that is how you'll get the probability distribution correct okay so all of this is known but it's worthwhile to emphasize it in the context of the decoder only model fine so now for as I said for each of these guys you want to make sure that the probability is maximized for the correct output that's why you have t uh probabilities here log probabilities here and you want to maximize each of them and the way you encode that as a function you just maximize the sum of those right is that okay everyone right okay so that's how it is and remember that the attention mask has been applied here that's why you're able to take the entire paragraph as an input and at every time step you are sure that only the correct words have been seen or whatever it is allowed to see has been seen nothing else has been seen because of this triangular mask that you have used okay attention mask so now that the model is clear uh we'll do a few things now we know the basic architecture of the model which is a decoder only model so we'll first talk about what was the data that it was trained on and I should warn you that it's uh very very small as compared to what modern gpts or other llms are trained on but that's where we started so we'll talk about the data used for training GPT and then we'll also talk about the architecture in terms of what was the dimensions how many attention heads and so on and try to compute the total number of parameters that are there in the GPT model okay so this uses what is known as the book Corpus it contains some 7,000 unique books 74 million sentences and approximately 1 billion words right which is as I said very small as compared to what modern llms are trained on uh also it uses long range contigous text that means uh the sequence length t as we'll see later on was 512 and these 512 tokens were contigous because it has books so it just takes like 512 continuous tokens from a book and what that means is that it does not really Shuffle the sentences right so it is continuous text so the entire 512 context is one continuous text which it takes together and feeds to the Transformer and tries to predict each of those 512 text 512 tokens while respecting the boundaries that means it does not look at any of the future tokens with the mask right and it uses bite pair encoding so this uh tokenizer uh is something that we have not looked at yet we'll look at it when we study the BT model we look at what different token what kinds of tokenizers exist but you could just think of it as a way of tokenizing the input sentence so if I'm saying um I am going home today then a simple tokenizer is that wherever you see a space you take that as a token right but you could do something more interesting like you could think of frequent suffixes like ing as separate tokens themselves so then you would split this as I am go in home today right so that would become six tokens right so we'll see what a tokenizer does but for now you can just think of it as that wherever you see a space that's a new token so when I say 512 tokens in our layman terms we just mean that 512 words were passed to the model but we'll refine this understanding as we go along and the vocabulary size was 40,000 what does that mean that when I said it's predicting a distribution over the vocabulary you should know how many unique items exist in the vocabulary right so it's just like operating a distribution over the color of a ball this our classic example of is what's the probability of a being a red ball white ball green ball so you know need to know how many colors are there right and then you will predict a probability for each of those colors similarly you could think of uh the token being a random variable which can take 4,478 values each of these is a unique word in the English uh vocab right uh and the embedding size was 768 so the word embedding that you pass as input was 768 dimensional and so yeah so we have some details here we'll look at few more details about the model so it contains 12 decoder layers right so there were 12 Transformer blocks one after the other uh its Contex size was 512 that means this capital T that we have been talking about the sequence length was pi12 okay that means it can see 512 inputs at one go right attention heads were 12 that means every layer had 12 attention heads in our initial discussions we had seen eight attention heads two attention heads so it had 12 of those uh the feed forward uh hidden layer had uh size is 768 cross4 yeah so 768 is the model size right which was the input embedding size so every uh layer right is producing a 768 dimensional output okay so even the attention head inside produces a 768 dimensional output then you have a feet forward Network which makes us three 3072 and then again shrinks it back to 768 right so that's what this feed forward hidden layer size was 307 does that make sense it just says that it was four times the model size right so this you chose to do a four times expansion and then again brought it back to 768 so this figure right so this is your FFN which takes the 768 dimensional input which is the representation computed by the 12 heads right so this is actually 64 into 12 each head produces a 64 dimensional output then you concatenate it so you get a 768 dimensional output which matches whatever your input Dimension was right that's what we saw on the previous slide that D model was 768 so each of these guys everywhere would be 768 in between through the for feed forward neural network layer you do this transformation of 768 becoming 3078 and then again shrinking to 716 right so the hidden layer of the trans feed for network is of size 307 uh it's not you need to do this this is yet another transformation you're doing and a lot of this is like uh empirical in terms of hyperparameter right you could try it's uh no one could argue that if you had done 2048 what would happen it's not an argument to be made it's just some choice that they made and we're just telling you what the choice is right there's no clear argument for why it should be 3078 not 1024 why not that's 204 yeah yeah it's part of the empirical evidence that they have right okay and they used uh gilu as the activation function now we'll see where does the activation function come up right because in the attention heads there is no activation function you just have the soft Max to compute the attentions so where does the activation function come the activation function comes in the feed forward Network so we'll see where exactly it comes and they use Dropout layer normalization and residual connections which helped in Faster conversions during training right so where each of these was put is what we'll see next right so this is what a sample data looks right so this is actually 52 tokens the highlighted part that you see there this is 512 tokens where I've treated every token as just space separated right so actually tokens are more than that because uh we use bite P encoding but for now as I said for our shallow understanding let's think of anything separated by space as a token right so this is the 512 sized input that I'm giving it so I give this entire thing to the Transformer and at the output I expect it to predict the probability of the corresponding V does that make sense right so let's see how that happens so you have the first Transformer block and go at the Bell Labs Hamming so this is the actual paragraph This is the 18th token research this is the 351st token and you continue this till you get to the 512th Token or 511 if you're 0 numbering so that's the last token in the 512 sized sequence okay this entire thing is going to get fed to the Transformer block when I say this entire thing is going to Fed what I mean is that for each of these words you will look up the word embedding and feed the word embedding through the Transformer BL correct right so that's the input to the network so this input would be T cross D model right which was 768 right T cross D model is what I'll write okay so each of this is D model size right each of these embeddings is of size D model right and you have t such embeddings because your sequence length is capital 3 so your input size is T cross D model does that make sense that means at every layer you will be producing a t cross D model sized output till the last layer and at the last layer you'll then convert it to soft Max so you'll be predicting capital T probability distributions does that make sense everyone huh it's masked yes because it's masked we are able to do this in parallel right because we don't we are sure that at every stage I'll not look at anything which is corrupted right so that's why I'm able to do this in parallel this entire input is going through the network in parallel all the probabilities are getting computed in parallel because I have ma mask that's the whole point of masking right so this is what one training batch looks like right I have a batch of say 512 tokens okay uh see that's not really a bad this is one training example of sequence length 5 and2 I could have a batch on top of this right so I could give 64 such sequences in patter right but for now let's compute uh consider that our bat size is one that means I have one sequence of 512 tokens right and this entire thing is traveling through the network all the computations are happening in parallel with the mask applied and then I finally compute the probabilities at the end okay so let's look at the entire thing so this is my 51 tokens they will go through this first Transformer blocks M uh m multi-ad m attention it's a what does that mean and then they'll go to the feed forward Network so this is what the multi-head master attention looks like right so this is the first head right this is the 12th head okay right so the first head will take all these 512 tokens it will generate the Q KV vectors do this entire op operation and again give me 5 and two vectors does that make sense right and what would be the size of each of those vectors it is D model divided by the number of heads that I have which is 12 right which in this case would be 64 okay so each of these heads is producing a 64 dimensional vector and then I'm going to concatenate them to get back my 768 dimensional Vector does that make sense no they're all taking the same input right so there are uh 512 input embeddings ped here for each of these you will compute the Q Vector the K Vector the V Vector right then using our standard uh attention equation which is this block for you will use the qkv to generate a new 64 dimensional embedding for each of these guys right that is what the first head will do in parallel the second head will also do that with its own WK WQ WQ all the 12 heads will do this in parallel they will generate this 64 dimensional refined representation for the input which is mindful of the other inputs by paying attention and appropriate masking right so it has generated those 12 64 dimensional vectors for each of the 512 tokens right so you have 512 tokens for each of them a 64 dimensional embedding is being generated by each of the 12 heads so this is 768 and that is what is getting concatenated so then finally you'll get 512 such concat concatenated vectors of size 768 does that make sense yes so you have 512 inputs so always have this mind view of Transformers right number of inputs same as number of outputs at every layer and to simplify things further we keep D model as a constant throughout the network that's why we call a D model so whatever is your input embedding size is the same as the output at every layer right right so that number does not change it's not that we cannot change it it's just that every Transformer architecture has that number as a constant does that make sense right so we had a 512 cross 768 input it went through this m attention and gave us a 512 cross 768 output then each of those 768 guys went through this linear transformation this is not the feat for Network this is the linear transformation within the M multihead attention right this was the wo Vector that we had right and then we again get a 512 cross 768 uh emings right for each of the 512 guys we get 768 mics 768 dimensional emic does that make sense yes no any questions on this these are all different attention heads each having their own WQ WK WV parameters yes one layer working parall yes yes right okay so this is one layer working in parallel 12 attention heads giving you the output this is still the multihead attention only we still have to deal with the feed forward Network right so after you have paid attention to everyone youve got 512 refined representations for the 512 input tokens each being of size 768 okay now after that let's see what happens after that you have the layer normalization and the residual connection remember that you had we had this layer normalization in Transformers right and then you have the residual connection which ensures that even if you have a very deep neural network the gradients still have a way to flow back which is the same as what we had in the residual networks okay uh okay they also use Dropout here right so they had said that they use Dropout layer normalization and residual connections so I'm just showing you the positions of where they use the Dropout and where they use the layer nominal and the residual connection okay this residual connection of course comes back from the input embeddings right so what is happening here is that you started off with an X right you computed some hes for that you started off with these X's right why are you all lost are you lost do I go slowly or something no you're all okay right and this is H so X was 512 tokens into 768 Dimensions H was also 512 into 768 Dimensions right and then what does residual connection means you are connect Computing H Prime which is x + H because you don't want to forget the original you just want to make sure that the gradients flow through that channel also which skips the entire Transformer block and propagates the gradients back to X and that's the whole skip connection idea okay that's what is happening in the Gory details of just the mask attention now we have got this H Prime which is again 512 representations of size 768 each this has to go through a feet forward neural network so let's see what happens there so now this goes to a feed forward neural network remember there there are five I can't I'll repeat it as many times as I need to there are 512 of these arrows right one corresponding to every token I'm looking at one of those guys now and telling you what happens in the feed forward neural network right so that goes through this is the input now to the feed forward neural network which was 512 dimensional it Maps it to what was that 30 72 and then again Maps it back to 512 so that your final output coming out is again sorry sorry sorry sorry sorry sorry uh yeah sorry the input was 768 maps to 3072 and again comes out at 768 this is for one of the guys the same process gets repeated for all the 512 guys so you have 512 cross 768 output is that okay right okay and u yeah and this is the point where you use the G activation function right so the hidden layer has the G activation function so far nowhere else we have used any activation function right so the feed forward hidden layer has the G activation function then after the feed forwards output you again apply Dropout you apply layer normalization and you again have the residual connection this residual connection coming from here this was the input to the block so always the input gets added back to the out outut clear okay so this is how the whole network looks like or sorry one block of the network looks like so we have we have seen that in gory details so just want to take a couple of minutes again to repeat what is happening right so you're taking a very large piece of continuous text which is 512 tokens feeding it through the Transformer layer right and at every layer you are again producing 512 outputs till the last layer and the last layer you applying soft of Max to convert these into 512 probabilities what are these probabilities you have X1 X2 given X1 X3 given X1 comma X2 x 512 given all of the others right so those are probability distributions that you are predicting all of this is happening in parallel and the mask is ensuring that you don't see anything which you're not allowed to see okay and these are 12 such blocks which are there okay so now let's count the number of parameters that are there in the network so the embedding is V cross embedding Dimension so we said embedding Dimension is 768 which is the same as D model and there were 40,000 words in the vocabulary 4,478 so this multiplication gives you some 31 million parameters okay these I of course not delve on to the answers here I'll just say okay this is 31 million and you just take my word for that uh then they have the position embeddings how many position emings you have you have a sequence length of 512 so for each of the 512 positions you will have a corresponding responding embedding and that embedding again has to be the same size as your vocabulary embedding so again 768 so for 512 positions you have 768 dimensional embeddings this is not much3 million parameters so total 31.3 million only in the input layer right the embedding and the positional embedding then unlike the previous Transformer that we had seen where the positional embeddings were just sinusoidal initialized and they were not learned now they initialize it and they also learned along with the other parameters of the network right so you could think of this 512 CR 768 Matrix as a parameter Matrix which is initialized in a certain way but then it'll get trained just as the other parameters in the networ okay then we look at the attention parameters so you have three matrices WQ WK WV right each of them takes a 768 dimensional input converts it into a 64 dimensional input so the Matrix would be 768 cross 64 uh this is per attention head and you have three such matrices so 3 into 768 CR 64 and then you have 12 such heads so I'll multiply this by 12 so this gives me 1.7 million parameters for the attention head in one layer okay then you have the linear layer which was W so that just takes a 768 so after you concatenate you have that 768 dimensional Vector then you multiply it by wo and again get a 768 dimensional Vector so that wo is going to be 76 8 768 which is6 million parameters and for all the 12 blocks you will have 1.7 million plus6 million which is 2.3 million multiplied by 12 so you have 27.6 million parameters for the attention uh block right the multi head M multi-ad self attention block will have 27.6 million parameters summed up across all the layers okay then let's look at the feed forward layer so feed forward layer has what does the feet forward layer do let's just see that it takes 768 transforms to 3072 again transforms to 768 so this is going to be a 768 cross 3072 Matrix then this is going to be a 3072 cross 768 Matrix right that's why you have two matrices of this Dimension okay then you will have 3 072 biases here and 768 biases there okay that's why you have this and so this sums up to 4.7 million parameters yeah so each of these guys would have a bias so that's why you have 3072 each of these guys will have a bias that's why you have 768 okay if you do this for one block it's 4.7 million so you multiply by 12 blocks you get 56.4 million parameters right so then you have embedding layer had 31.3 million parameters attention layer had 27.6 million parameters feet forward layer had 50 6.4 million parameters the total number of parameters is roughly 116 million now I think yeah one thing which we did not do is that at the end you get 768 dimensional outputs right that you again transform to a 40, 478 dimensional output right because this is the probability distribution right so you'll again need a 768 cross 4478 Matrix here but at the input you already had that mat Matrix right correct so these matrices can be shared that's why I have not counted them again is that okay this is called the input embedding Matrix and this is called the output embedding Matrix these could be shared so I'm not counted them okay if you count them if you don't share them then some parameters will get added okay okay so the gpt1 as I said was like really a smallish model as compared to what modern models are 117 million parameters nowadays we talk about 7 billion 13 billion 70 billion parameters right so is very very small as compared to that but this was the initial model that had come out okay
[Music] so the question is batch normalization has been proven to be successful in CNN in uh it's also proven to be successful in feed forward neural networks now can we apply it to Transformers okay again the same concept applies in principle in Transformers that that you have an input these was our original sentence I am home today or whatever it was it passes through the first layer you get some outputs right then it passes through the lay second layer you get some outputs and this continues for as many layers of the encoder and decoder that you have yes so the same concept applies you have an X then you have an H1 you have an H2 and so on correct right so if the same kind of abstraction applies here also that you're taking an input doing a c series of Transformations and here you guarantee some normal distribution sorry some standardization so can you do the standardization at each of these places also right so that's the question that we are asking correct everyone gets this yes okay so the answer is of course yes however there are some limitations to batch normalization and let's see what that is right so now the accuracy of the estimation of the mean and the variance depends on the bat size if I take a very small bat size I am not going to get a very good estimate for the me right so the best estimate is take the entire Matrix X as we did but as I said for hes we cannot do that because we only have access to one batch at a time now if the batch size is very small then our estimate of the mean and variance will not be good right so that's one problem that we have uh and we can't use a very small bat size of say one to do this obviously if you take a size of one it's just the same uh element there there's no concept of like mu is equal to X and sigma is equal now other than this limitation right which is like obvious to understand empirically also people found that when you use batch normalization the performance degrades in case of various NLP task this was an empirical observation that people found and there was also a systematic study that validated that this empirical observation is actually true and a new normalization method was published which is known as power normalization power Norm but fortunately there is a simpler technique called layer normalization that works very well and in terms of conceptual understanding if you have understood batch normalization it would be very easy to understand layer normalization right we'll work with the same abstraction of that Cube that we saw and the main idea would be instead of accumulating along the batch you'll accumulate along the layer right so that's what we will do okay so B normalization we understand we also agree that the same idea can be should be applied to Transformers empirically it did not work very well systematic study also showed it's not the best idea or maybe better normalization techniques can be used but then a simpler normalization technique emerged which is layer normalization right and that's what we're going to learn okay okay so now uh so this is the Elli layer okay fine this is one layer now now earlier what was I doing okay I think this diagram will explain it better earlier I was taking the average by doing the following right this was my batch size this was my bat size right and I was aggregating across this to compute the me right now let's for a minute assume that my bat size is just one okay if that's the case this aggregation does not make sense right so now what is this this is Su H1 right so the output at the first after the first encoder layer in the Transformer okay and my bat size was just one so this is all I have now I want to perform normalization what will I do so this is the entire layers output do you see that is the entire lay as output I'll just do layer normalization that means I'll compute mu by accumulating along this damage does that make sense right so now even if your bat size is small your number of neurons in one layer is typically High what was that that was D model if you remember right which was 512 or 1024 768 that already looks like a very large reasonably large value right and now even if your bat size is 16 or 32 you still aggregating along these 512 entries so your mu and variance would still be good and you're standardizing across this does that make sense that's what layer normalization does it standardizes across all the outputs in a given layer the standardization does not happen along the batch it happens along the layer is that okay everyone right fine okay cool so that's all that layer normalization does and then the rest of the formula all it just a matter of how you're Computing the mean and the variance uh so this is how you're Computing it right so here you have Capital H which is the size of the number of neurons in the layer and then you similarly compute the mean the mean and the variance again standardize and then again open the door for getting out of the standardization if you want to okay so all of these equations look the same as what we saw in Bat normalization it's just that how are you Computing a mean along the batch or along the layer okay so that's all there is to layer normalization okay so now that we have looked at both residual connections and layer normalization uh we are ready to look at the full encoder Network now this is the complete encoder layers so still till now we assume that it contains a multi-head attention and a feed forward Network and these are the only two things we had seen multi-head self attention and feed forward Network now we also do the following what have I added here add and layer normalization right so how should you look at this I'll tell you how to look at it right so this is my one layer of the encoder remember this was my input these were just the word embeddings of whatever words I had in my sentence right and after this encoder layer I was getting a updated representation for these guys yes no yes okay now the first thing that is happening is that all of these are going through that self attention layer okay and what does self attention do it computes a new representation for the first token by taking a weighted average of all the other tokens yes of course after those q k v vectors but in conceptually it just takes a weighted average of all the uh tokens right some representation of all the Tok so that's my new transformed X right so if I call this x let's call this x hat okay and I have a transform output for all the tokens does that make sense right now I'm going to do add and layer normalization what does that mean I'm going to say that whatever comes out here I'm going to write it as x + x hat X was my original input right so this again I'm going to feed here and just add with whatever output I have received does that make sense that's how residual connection works right whatever was your input right you just say that my revised output after adding the residual connection is going to be X plus whatever transformation has happened so this is the transformation and this is the original input right so now you get this X Plus X hat here okay and this then you pass through layer normalization so it does the layer normalization on top of this does that make sense right so it takes this xtia normalizes across the layer and then comput some X Prime now right so X Prime is layer normalization applied on top of X Del is that okay everyone yes sir yes okay so that's what this block is trying to tell you now whatever xtia you get that you pass to the feed forward Network so this is your xtia right so then that was the feat forward Network and you get some say x double TAA from here okay after that you again add the ADD and layer normalization that means your final output would be x triple Tilda is equal to the original XA right plus X Theta does that make sense and then you again compute I'm running out of symbols okay is equal to layer normalization of this is that okay everyone yes so this is what is happening in detail in the network right so after every attention layer you're adding and layer normalization after every feed forward Network you're adding and lay normalization okay so this is what the encoder complete picture looks like and this is what the full Transformer architecture now looks like okay so we are ready to look at it in its full Glory so you had the input embeddings okay you are adding the positional embeddings to it okay so if this is 512 dimensional you are adding those five12 dimensional sydal positional embeddings to it so what you get out of here is again a 512 dimensional Vector right that you pass through the multi-ad attention right and what do you pass to the multi-ad attention you first do K V and Q right so from every guy here you calculate three guys k q and V by using those corresponding matrices right is that okay right that you pass to the multi attention now whatever the multi-ad attention gives you again add the original input to it right and then you do layer normalization that you pass to the feed forward Network whatever the feed forward Network gives you again do the add the original input to it and then do layer normalization right and you do this six times 12 times whatever is the number of encoders right this is one encoder and you do this n times that right n is the number of encoder layers that you have now coming to the decoder again you have the output embedding where did we have the embedding in the decoder so remember you had the decoder you started with go right then you predicted I okay then what do you find push as the input the embedding of the word I correct yes right what else will you push right I mean you cannot pass the word I it has to be some Vector say it's the embedding of the word I so that's what is being said here that also gets added to a positional embedding because again in the decoder you need to have a sense of what the position is right so add it to the position then once you do that the same process right so you compute the multi-ad attention then add the original input normalize it okay this again you as we had said right so this becomes the query and the key and value come from the encoder right then again whatever transformation you compute you're Computing a new representation for the decoded tokens so those again you add and then normalize is it okay feed forward Network add and normalize and then finally you have the linear layer and the soft Max layer is this clear right so now we understand the full Transformer architecture all its components in all its Glory the main components are multi-head attention right which was that softmax formula which we saw Q transpose K into V then you have the feed forward Network which is a simple one hidden layer uh Network then you had the masked multi-head attention and we understood why we need masking because we want to do teacher forcing as well as because in practice you don't have access to the remaining tokens so you need to Mas them out you are not it's not legal to look at them so we did this masking we implemented it using a matrix which had zeros and minus Infinities uh then again we had the feed forward layer at the end we wanted to predict a distribution over the probability over the vocabulary so that's where we need the linear layer and the soft Max layer right and then after all of this to improve or to stabilize the training ensure conversions happens fast we had to add the residual connections and we added the layer normalization right so that is all that is to be said about the Transformer architecture so I'll end this first lecture here uh where we have looked at the Transformer architecture in detail now going forward we'll start looking at some of the early large language models so I'll start that tomorrow thank you